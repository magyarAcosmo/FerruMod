[package]
name = "ferrumod_etl"
version = "0.1.0"
edition = "2021"

[dependencies]
noodles = { version = "0.80", features = ["bam", "sam"] }
polars = { version = "0.38", features = ["parquet"] }
rayon = "1.10"
anyhow = "1.0"
